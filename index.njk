---
layout: layouts/default
pagination:
  data: collections.blog
  size: 5
---

<div class="aspot">
  <p class="title-major container">
    <strong>actree</strong> ist eine Community, die sich für einen nachhaltigen Lebensstil einsetzt. <a class="aspot__button" href="/actree/verein">Lern mehr über uns!</a>
  </p>
</div>
<section class="home page-container container">
  <div class="home-latest">
    <div class="home-latest-blogs">
      <h4 class="title-list">Vom Blog</h4>
      {%- for post in collections.blog | latest10 %}
        <section class="mini">
          {% if post.data.image %}
            <a class="image-link" href="{{ post.url }}">
              <img class="image" src="{{ post.data.image }}" alt="{{ post.data.title }}">
            </a>
          {% endif %}
          <header>
            <div class="category">{{ post.data.category }}</div>
            {# {{!-- <div class="category">{{category.name}}</div> --}} #}
            {# <h2 class="title"><a href="{{this.url}}">{{ this.data.title }}</a></h2> #}
            <h2 class="title"><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
          </header>
        </section>
      {%- endfor %}
      <a href="/blog" class="button">Weitere Artikel</a>
    </div>
    <div class="home-latest-entries">
        <h4 class="title-list">Neueste Einträge</h4>
        {%- for entry in collections.entry | latest10 %}
          <div class="entry-mini ">
              <a href="{{entry.url}}" class="entry-image">
                  {# {% if entry.data.image %} #}
                      <img class="entry-img" src="{{ entry.data.image }}">
                  {# {% endif %} #}
              </a>
              <div class="entry-main">
                  <a href="{{ entry.url }}" class="entry-title">
                      <h4 class="title-major">{{ entry.data.title }}</h4>
                  </a>
                  <div class="entry-meta ">
                      <ul>
                        {%- for tag in entry.data.entrytags %}
                          <li><a href="{{tag | slugify}}" class="tag">{{ tag }}</a></li>
                        {%- endfor %}
                      </ul>
                  </div>
              </div>
          </div>
        {%- endfor %}
      </div>
  </div>
</section>
